apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-artifactory-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-artifactory-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-artifactory-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
